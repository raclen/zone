---
title: "文字闪烁"
description: "注意需要引入jquery"
pubDate: 2014-05-14T02:56:11.000Z
issueNumber: 10
issueUrl: https://github.com/raclen/zone/issues/10
tags: ["legacy", "JavaScript"]
author:
  name: "raclen"
  avatar: "https://avatars.githubusercontent.com/u/7697758?v=4"
draft: false
---


注意需要引入jquery
```javascript
function shakeTopText(id){
		$(id).css("color", randomColor());
	}

	function loadTopText(id, time) {
		setInterval("shakeTopText('"+id+"')", time);
	}

	function randomColor() {
		var R = parseInt(255 * Math.random());
		var G = parseInt(255 * Math.random());
		var B = parseInt(255 * Math.random());
		R = R.toString(16);
		G = G.toString(16);
		B = B.toString(16);
		if (R.length == 1)R = "0" + R;
		if (G.length == 1)G = "0" + G;
		if (B.length == 1)B = "0" + B;
		return("#" + R + G + B);
	}
window.onload=function(){
	loadTopText("#wyj",500);

	}
```

