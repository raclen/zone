---
title: "node上传图片"
description: "开始用的kindeditor编辑器，上传图片不成功，就换了   这是node代码，需要注意的是uploadDir指向的路径文件夹要真实的存在，它不能在不存在的时候给你创建一个，"
pubDate: 2015-11-18T03:10:00.000Z
issueNumber: 64
issueUrl: https://github.com/raclen/zone/issues/64
tags: ["legacy", "JavaScript", "Node.js", "nodejs"]
author:
  name: "raclen"
  avatar: "https://avatars.githubusercontent.com/u/7697758?v=4"
draft: false
---


开始用的kindeditor编辑器，上传图片不成功，就换了[wangEditor编辑器](https://github.com/wangfupeng1988/wangEditor)
 ```javascript
  "express": "^4.13.3",
  "multiparty": "^4.1.2",
 ```
这是node代码，需要注意的是uploadDir指向的路径文件夹要真实的存在，它不能在不存在的时候给你创建一个，
 ```javascript
 app.post('/uploadImg', function(req, res, next) {
     var form = new multiparty.Form({uploadDir:'./public/upload'});
     form.parse(req, function (err, fields, files) {
         if (err) {
             throw err;
         }
         console.log(files)
         var image = files.file[0];
         var path = image.path;
         path = path.replace(/\\/g, '/');
         console.log(path)
         var url = 'upload' + path.substr(path.lastIndexOf('/'), path.length);
         res.send(url);
     });

 });
 ```

