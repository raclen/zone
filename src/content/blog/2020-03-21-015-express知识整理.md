---
title: "Express知识整理"
description: "路由 和同时存在，谁写在前面执行谁，如果会执行下一个，否则不执行下一个  中间件  模板 设置模板文件为html  设置静态文件目录  这里可以设置多个，第一个参数为路由字符串 错误处理"
pubDate: 2020-03-21T07:32:13Z
updatedDate: 2020-03-21T07:33:24Z
issueNumber: 15
issueUrl: https://github.com/raclen/raclen.github.io/issues/15
tags: ["idea"]
author:
  name: "raclen"
  avatar: "https://avatars.githubusercontent.com/u/7697758?v=4"
draft: false
---

### 路由
`app.all()`和`app.get()`同时存在，谁写在前面执行谁，如果`next()`会执行下一个，否则不执行下一个
```javascript
app.all('/secret', function (req, res, next) {
  console.log('Accessing the secret section ...');
  next(); // pass control to the next handler
});
```
### 中间件
```javascript

let myLogger = (req,res,next)=>{
  log('do something');
  log('也可以修改res对象的值');
  next();//这个一定要有
}
app.use(myLogger)
```
### 模板
设置模板文件为html
`app.engine('.html', ejs.renderFile);`
设置静态文件目录
`app.use('/vbk/static/', express.static(path.join(__dirname, 'dist')));`
这里可以设置多个，第一个参数为路由字符串
### 错误处理
```javasctipt
//错误处理就像是中间件多一个参数error
app.use(function(err, req, res, next) {
  console.error(err.stack);
  res.status(500).send('500。。。。。!');
});
```


---

💬 **[在 GitHub Issue 讨论这篇文章](https://github.com/raclen/raclen.github.io/issues/15)**
