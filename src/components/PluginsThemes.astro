---
// 洛雪插件 & 主题包安装说明。简洁引导，不堆砌细节。
---

<section class="section" id="plugins">
  <div class="container">
    <div class="section-head center reveal">
      <span class="eyebrow">扩展生态</span>
      <h2 class="section-title">插件 & 主题</h2>
      <p class="section-desc">
        通过插件扩展在线音源，用主题包定制你的专属界面风格。
      </p>
    </div>

    <div class="plugins-grid">
      <article class="plugin-card glass reveal">
        <div class="plugin-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
            <path d="M9 2v5M15 2v5"/><path d="M6 7h12v3.2a6 6 0 0 1-12 0V7Z"/><path d="M12 17v4.5"/>
          </svg>
        </div>
        <h3>洛雪插件系统</h3>
        <p>
          AnhePlayer 默认不内置音源，需安装洛雪插件扩展。<br/>
          在插件管理中导入本地 <code>.js</code> 文件，或添加以 <code>.js</code> 结尾的 https 链接。<br/>
          插件声明的 source 将作为播放解析源，支持多音质与音源重定向。
        </p>
      </article>

      <article class="plugin-card glass reveal">
        <div class="plugin-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
            <path d="M12 2.5a9.5 9.5 0 1 0 0 19c.83 0 1.5-.67 1.5-1.5 0-.4-.16-.76-.42-1.03a1.5 1.5 0 0 1 1.06-2.55H16a5 5 0 0 0 5-5C21 6.7 16.97 2.5 12 2.5Z"/><circle cx="7.5" cy="10.5" r="1"/><circle cx="12" cy="7.5" r="1"/><circle cx="16.5" cy="10.5" r="1"/>
          </svg>
        </div>
        <h3>主题包定制</h3>
        <p>
          内置浅色主题和纯黑主题，支持导入本地或远程主题包（<code>.mftheme</code>）。<br/>
          主题包可自定义颜色、背景、字体与界面布局，打造属于你的音乐播放器。<br/>
          完整主题开发文档请参阅 GitHub 仓库。
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .plugins-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  .plugin-card {
    padding: 36px 32px;
    transition: transform 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
  }
  .plugin-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-strong);
    box-shadow: var(--glow-soft);
  }
  .plugin-icon {
    width: 56px;
    height: 56px;
    display: grid;
    place-items: center;
    border-radius: 14px;
    margin-bottom: 20px;
    color: var(--brand-bright);
    background: linear-gradient(135deg, rgba(32, 167, 255, 0.16), rgba(139, 92, 246, 0.16));
    border: 1px solid rgba(32, 167, 255, 0.22);
  }
  .plugin-icon svg {
    width: 28px;
    height: 28px;
  }
  .plugin-card h3 {
    font-size: 1.35rem;
    font-weight: 700;
    margin-bottom: 14px;
  }
  .plugin-card p {
    color: var(--text-dim);
    font-size: 0.98rem;
    line-height: 1.7;
  }
  .plugin-card code {
    font-family: var(--font-mono);
    font-size: 0.9em;
    padding: 2px 6px;
    border-radius: 4px;
    background: rgba(32, 167, 255, 0.15);
    color: var(--brand-bright);
  }

  @media (max-width: 840px) {
    .plugins-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
